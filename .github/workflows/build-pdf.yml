name: Build PDF

on:
  push:
    branches: [main]
    paths:
      - "report.tex"
      - "graphes/**"
      - "annexe/**"
      - ".github/workflows/build-pdf.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Compile report.tex
        uses: xu-cheng/latex-action@v3
        with:
          root_file: report.tex
          args: -pdf -file-line-error -halt-on-error -interaction=nonstopmode

      - name: Compile annexe/annexe.tex
        uses: xu-cheng/latex-action@v3
        with:
          root_file: annexe.tex
          working_directory: annexe
          args: -pdf -file-line-error -halt-on-error -interaction=nonstopmode

      - name: Commit updated PDFs
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Auto-build PDF(s) from .tex sources [skip ci]"
          file_pattern: report.pdf annexe/annexe.pdf
